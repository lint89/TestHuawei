/*********************************************************
* 题目描述

* 写出一个程序，接受一个十六进制的数值字符串，输出该数值的十进制字符串。（多组同时输入 ）

* 输入描述:
* 输入一个十六进制的数值字符串。


* 输出描述:
* 输出该数值的十进制字符串。

* 输入例子:
* 0xA

* 输出例子:
* 10
 *********************************************************/


#include <stdio.h>
#include <math.h>

int main()
{
	int a, b, c, d;
	scanf("%d", &a);

	d = a;
	for (b = 2; b < sqrt((float) d); )
	{
		if (a % b == 0)
		{
			printf("%d ", b);
			a /= b;
			continue;
		}
		b++;
	}
}